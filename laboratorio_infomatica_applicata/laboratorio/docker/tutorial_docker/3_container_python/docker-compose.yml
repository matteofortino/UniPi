version: "3.1"
services:
  web:
    # replace username/repo:tag with your name and image details
    image: container_python:latest
    # image: python-docker can be substituted by this build (using the Dockerfile in the same folder)
    #build: ./
    ports:
      - "8080:5000"

    env_file:
      - ./.env

